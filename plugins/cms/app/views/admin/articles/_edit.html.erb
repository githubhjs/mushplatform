<% form_remote_tag :url => 'articles/update', :before => fckeditor_before_js('body','') do -%>
<input type="hidden" name="page" value="<%= @page ? "1" : @page %>">
<h2><%= t 'text.article.manage_articles' %> - <%= @channel.name %> </h2>

<ul class="subsubsub">
  <li><a class="current"><%= @article.title %></a></li> | 
  <li><a class="current"><%= link_to_remote (t 'text.cancel'), :url => {:action => 'index', :channel_id => @channel.id, :standalone => @standalone ? @standalone : ''}, :class => 'cancel' %></a></li>
</ul>

<%= render :partial => 'form' %>

<div id="ajax-response"></div>

<div class="tablenav">
    <br class="clear" />
</div>

<br class="clear" />
<% end -%>
